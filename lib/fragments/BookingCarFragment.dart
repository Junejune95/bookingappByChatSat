import 'package:flutter/material.dart';

class BookingCarFragment extends StatelessWidget {
  const BookingCarFragment({Key? key}) : super(key: key);


  @override
  Widget build(BuildContext context) {
    return Scaffold (
      appBar: AppBar(title: const Text('Car')),
    );
  }
}
