class DistinationModel {
  String id;
  String title;
  String image;
  String content;
  
  DistinationModel({
  required  this.id,
   required this.title,
   required this.image,
   required this.content,
  });
}
